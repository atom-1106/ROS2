%YAML 1.2
---
launch:
  - arg:
      name: config_file
      default: $(find-pkg-share cat_apps)/config-example.xml
      description: Path to baseline pub/sub XML configuration file
  - node:
      pkg: cat_apps
      exec: BaselineSubscriber
      output: screen
      args:
        - $(var config_file)
